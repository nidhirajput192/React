import React from 'react';  // react library
import ReactDOM from 'react-dom/client';   // react DOM  library

import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 
    <App />
  
);

